const appName = 'FX Guru';

const pushNotificationTitle = 'Login Alert';
const pushNotificationBody = '';

const Map pushNotificationData = {
  'title': pushNotificationTitle,
  'body': pushNotificationBody,
};

const mailNotificationTitle = 'Login Alert';
const mailNotificationBody = '';

const Map mailNotificationData = {
  'title': mailNotificationTitle,
  'body': mailNotificationBody,
};
